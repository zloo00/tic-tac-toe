MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=change-me
MONGO_INITDB_DATABASE=tictactoe

# Mongo connection string for the API container (hostname matches docker-compose service)
MONGODB_URI=mongodb://admin:change-me@mongodb:27017/tictactoe?authSource=admin

# Development secret only; replace in production
JWT_SECRET=dev-secret

