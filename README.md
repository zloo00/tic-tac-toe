# 🎮 Tic-Tac-Toe Online

Онлайн-платформа для игры в крестики-нолики 1v1 с realtime обновлениями.

## 🏗️ Структура проекта

```
tic-tac-toe/
├── client/          # Next.js App Router + TailwindCSS
├── server/          # Express + Apollo GraphQL
└── docker-compose.yml
```

## 🚀 Запуск

```bash
docker-compose up
```

Это запустит:
- MongoDB на порту 27017
- Server на порту 4000
- Client на порту 3000

## 🔐 Переменные окружения

1. Скопируйте `.env.example` в `.env` в корне проекта.
2. При необходимости поменяйте учетные данные (значения по умолчанию совпадают с `docker-compose.yml`).
3. В `MONGODB_URI` оставьте хост `mongodb`, чтобы сервер мог обратиться к контейнеру базы.

## 📦 Технологии

- **Client**: Next.js 14 (App Router), TypeScript, TailwindCSS, Zustand
- **Server**: Node.js, Express, Apollo GraphQL, TypeScript, MongoDB
- **Database**: MongoDB
- **Realtime**: GraphQL Subscriptions

## 🌱 Демо-данные

1. Перейдите в `server/` и выполните `npm install`, если ещё не делали этого.
2. Заполните базу тестовыми пользователями/комнатами:  
   ```bash
   cd server
   npm run seed
   ```
3. Скрипт очистит коллекции и создаст аккаунты:
   - `alice@example.com / password123`
   - `bob@example.com / password123`
   - `carol@example.com / password123`

Комнаты: `ALPHA1` (ожидает игрока), `BETA2` (идёт игра), `OMEGA3` (завершена).

## 🧪 Тесты

Серверная часть покрыта >20 Jest‑тестами (auth/game сервисы + движок).  
Запуск:

```bash
cd server
npm test
```

## ⚡ Сценарий для realtime-демо

1. Выполните `npm run seed` (см. выше), затем поднимите MongoDB + сервер + клиент (`docker compose up` или отдельные `npm run dev` в `server/` и `client/`).
2. В двух окнах браузера залогиньтесь как `alice@example.com` и `bob@example.com`.
3. На первой вкладке откройте `/lobby`, нажмите «Watch realtime» у комнаты `BETA2` и перейдите в `/room/BETA2/game`.
4. На второй вкладке откройте `/room/BETA2/game` под вторым пользователем.
5. Делайте ходы на любой вкладке — доска, статус, список ходов и лидерборд будут обновляться мгновенно через `gameUpdated` и `roomUpdated` подписки.
6. Для демонстрации чата/ожидания откройте `/room/ALPHA1` и покажите, как второй игрок присоединяется: в лобби и «Room waiting» появятся изменения без перезагрузки.
